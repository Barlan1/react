import React from "react";
export function UserCard_message({user, age, city}){
  return(
    <div>
     <h2>User Card</h2>
     <strong>Name: {name}</strong>
     <P> Age:{age}</p>
      <strong> City: {city}</strong>
    </div>
  );
}
